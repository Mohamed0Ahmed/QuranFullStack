# Feature 010 — Real Import Run Summary

**Feature:** Quran Full I'rab Foundation
**Produced by:** Phase 5 polish
**Date:** 2026-06-17

## Prerequisites verified

| Requirement | Status |
| --- | --- |
| Staged package at `resources/import-sources/quran-full-i3rab/` | **PRESENT** (`manifest.json` + `sources/` + 4 JSON files) |
| Migration `20260617104912_AddQuranFullI3rab` generated | **DONE** (EF-generated) |
| Migration applied to target PostgreSQL | **DONE** (via `dotnet ef database update`, Api startup project) |
| Feature 002 foundation import (`quran_ayahs` = 6,236) | **VERIFIED** |

## Commands executed

```bash
# 1. Apply migration
cd Backend
ConnectionStrings__QuranDashboardDb="Host=localhost;Port=5432;Database=quran_dashboard;Username=postgres;Password=***" \
  dotnet ef database update \
  --project infrastructure/QuranDashboard.Infrastructure \
  --startup-project api/QuranDashboard.Api

# 2. First import (happy path)
ConnectionStrings__QuranDashboardDb="Host=localhost;Port=5432;Database=quran_dashboard;Username=postgres;Password=***" \
  dotnet run --project tools/QuranDashboard.DataImporter -- import-full-i3rab \
  --source /projects/Dashboard/App/resources/import-sources/quran-full-i3rab \
  --report-out /projects/Dashboard/App/resources/report/quran-full-i3rab

# 3. Rerun refusal check (no --force)
# Same command as step 2 — expected refusal, exit code 2, no mutation
```

## Console summary (first import)

```text
Full i'rab import completed successfully.
sources=4, entries=14513, ayahMappings=24944, distinctAyahs=6236, contentWarnings=0.
Report written to: /projects/Dashboard/App/resources/report/quran-full-i3rab
```

## Post-run database verification

| Check | Result |
| --- | --- |
| `quran_full_i3rab_sources` row count | 4 |
| `quran_full_i3rab_entries` row count | 14,513 |
| `quran_full_i3rab_ayah_entries` row count | 24,944 |
| Junction rows per source (`daas`, `darwish`, `jadwal`, `muyassar`) | 6,236 each |
| Distinct ayahs covered | 6,236 |

## Importer report (`full-i3rab-import-report.json`)

| Field | Value |
| --- | --- |
| `verdict` | `pass` |
| `persisted` | `true` |
| `forced` | `false` |
| Hard checks passed | 21 / 21 |
| Content warnings | 0 |
| Provenance warning | `FULLI3RAB-PROVENANCE-WARNING` present |
| `licenseStatus` / `provenanceStatus` / `usageScope` | `unknown` / `unknown` / `internal-only-until-cleared` |

### Per-source block counts (from `infoNotes`)

| Source key | Entry blocks |
| --- | ---: |
| `daas` | 3,633 |
| `darwish` | 1,387 |
| `jadwal` | 3,257 |
| `muyassar` | 6,236 |

## Rerun refusal verification

Second run without `--force` refused with message "Full i'rab tables are not empty. Re-run with
`--force` to rebuild them." Exit code 2. Row counts unchanged. Refusal output retained at
`full-i3rab-rerun-refusal-report.{md,json}`.

## Report locations

- **Gitignored run output:** `/projects/Dashboard/App/resources/report/quran-full-i3rab/`
- **Committed PASS snapshot:** `Backend/report/feature-010-quran-full-i3rab-foundation/full-i3rab-import-report.{md,json}`
  — genuine `forced=false` first-run pass (`verdict=pass`, `persisted=true`, 21/21 hard checks). The
  original first-run output was overwritten in the gitignored dir by the rerun-refusal verification
  below (same `--report-out`), so this snapshot was regenerated by truncating only the three
  `quran_full_i3rab_*` tables and re-running `import-full-i3rab` **without** `--force`
  (regenerated 2026-06-17T13:17Z). Identical data and counts.
- **Committed rerun-refusal snapshot:** `Backend/report/feature-010-quran-full-i3rab-foundation/full-i3rab-rerun-refusal-report.{md,json}`
  — the idempotency-guard refusal output (2026-06-17T12:50Z run), retained as evidence.

## Status

**PASS** — full staged-package import complete and verified.
