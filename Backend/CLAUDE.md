# Claude Backend Structure Router

Current Backend code is implementation truth.

- When adding, moving, splitting, or assigning responsibility to Backend files, load
  `Backend/.architecture/BACKEND_STRUCTURE.md` and
  `Backend/.architecture/CLEAN_ARCHITECTURE.md`.
- For other Backend work, inspect the code in scope without loading additional repository policy.
- Use `test-guard` only when the user explicitly names it or explicitly asks to use Test Guard.
