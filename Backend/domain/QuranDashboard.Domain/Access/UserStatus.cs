namespace QuranDashboard.Domain.Access;

// Stored as-is; do not reorder or renumber (would corrupt already-persisted rows).
public enum UserStatus
{
    Pending = 1,
    Active = 2,
    Disabled = 3
}
