// Global test setup for the Vitest unit-test runner (@angular/build:unit-test).
// The Angular TestBed is initialized by the builder before this file runs; this
// file only needs to enable the testing configuration of zone.js.
import 'zone.js/testing';
