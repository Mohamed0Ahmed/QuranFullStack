---
description: "Invoke the deploy-smoke skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `deploy-smoke` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
