---
description: "Invoke the speckit-converge skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `speckit-converge` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
