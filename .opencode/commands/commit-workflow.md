---
description: "Invoke the commit-workflow skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `commit-workflow` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
