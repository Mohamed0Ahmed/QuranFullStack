---
description: "Invoke the speckit-git-initialize skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `speckit-git-initialize` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
