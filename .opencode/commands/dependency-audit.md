---
description: "Invoke the dependency-audit skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `dependency-audit` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
