---
description: "Invoke the test-guard skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `test-guard` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
