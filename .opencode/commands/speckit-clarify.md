---
description: "Invoke the speckit-clarify skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `speckit-clarify` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
