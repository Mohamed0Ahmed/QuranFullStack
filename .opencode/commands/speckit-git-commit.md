---
description: "Invoke the speckit-git-commit skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `speckit-git-commit` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
