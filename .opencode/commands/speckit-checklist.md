---
description: "Invoke the speckit-checklist skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `speckit-checklist` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
