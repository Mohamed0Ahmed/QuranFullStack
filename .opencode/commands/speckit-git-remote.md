---
description: "Invoke the speckit-git-remote skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `speckit-git-remote` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
