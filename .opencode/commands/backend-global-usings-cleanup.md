---
description: "Invoke the backend-global-usings-cleanup skill"
---
<!-- managed-by-codex: opencode-skill-command-wrapper -->
Load the `backend-global-usings-cleanup` skill with OpenCode's native `skill` tool, then follow its instructions for the request below.

User request:
$ARGUMENTS
