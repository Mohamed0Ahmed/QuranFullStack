---
name: speckit-create-loop
description: >-
  Pointer for non-Claude agents. Canonical speckit-create-loop instructions live at
  `.claude/skills/speckit-create-loop/SKILL.md`. Use that Claude skill as the source of truth for
  the invocation contract (`path` + `scope`), the sequence preflight, the source-scope traceability
  file, the main-session stage order, the task and phase readiness gates, and the
  analyze/remediation convergence rule.
---

# Spec Kit Create Loop Skill Pointer

Canonical source:

- `.claude/skills/speckit-create-loop/SKILL.md`

Non-Claude agents should read and follow the canonical Claude skill file. Do not keep a second full
copy of the create-loop rules here; this file exists only to route agents to the single source of
truth.
