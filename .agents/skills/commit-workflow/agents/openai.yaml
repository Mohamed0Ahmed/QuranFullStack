interface:
  display_name: "Commit Workflow"
  short_description: "Perform requested monorepo Git operations safely"
  default_prompt: "Use $commit-workflow to perform the requested Git operation (inspect, branch, stage, commit, push, PR, or sync) safely from the monorepo root."
