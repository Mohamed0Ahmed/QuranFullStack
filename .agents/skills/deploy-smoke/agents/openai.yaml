interface:
  display_name: "Deploy Smoke"
  short_description: "Deployment preflight / local runtime smoke check"
  default_prompt: "Use $deploy-smoke for the requested deployment preflight or local runtime smoke check."
