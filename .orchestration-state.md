# Issue #149 orchestration state (persisted 2026-09-04)

NOT COMMITTED - working note for session recovery. Delete when program completes.

## Integration branch
`149-persistent-test-database`, tip `d26083ae` (Merge offline JWT validation fix for nested rehearsal runs)

## Closed (14)
#150 #151 #152 #153 #154 #155 #156 #157 #158 #159 #161 #165 #168 #171

## Open / in flight
- **#160** dispatched CODEX, relay `b6wb35gcb`, worktree `/home/mohamed/qdb-worktrees/issue-160`,
  branch `codex-issue-160-from-149` from `d26083ae`.
- **#162** dispatched CODEX, relay `b7uf4t4md`, worktree `/home/mohamed/qdb-worktrees/issue-162`,
  branch `codex-issue-162-from-149` from `d26083ae`.
- **#163** dispatched CODEX, relay `bobp0meer`, worktree `/home/mohamed/qdb-worktrees/issue-163`,
  branch `codex-issue-163-from-149` from `d26083ae`.
- 2026-09-04 USER INSTRUCTION: after #160, #162, #163 are landed+verified+CLOSED, **STOP**. Do NOT dispatch
  #164 #166 #167 #169 #170 or close root #149 without a new instruction.
- Integration tip `d26083ae`. Lane VERIFIED GREEN by orchestrator: exit 0, 9m33s, 0 failures,
  shard1 244/244 2.30min, shard2 46/46 3.96min.

## User instructions in force
- No degraded verification. Do not land or close any ticket whose database-backed verification has not executed.
- Keep local DB state as-is; never touch `quran_dashboard`.
- Continue with current parallel approach (up to 3 workers), CODEX one-line dispatch.
- 2026-09-04 (superseded the earlier no-new-tickets hold): finish #159, then complete ALL remaining
  tickets with codex as at session start.
## Test database capability (provisioned 2026-09-03/04)
- `quran_dashboard_test` on local PostgreSQL 18.6, login `mohamed` (NOSUPERUSER, CREATEDB, CREATEROLE, owner).
- 16 `GRANT SET ON PARAMETER` bootstrap applied by superuser (see TestRuntime README).
- 78 tables, head `20260826012918_AddQuranPhraseSearchIndex`, 12 markers healthy, catalogue healthy.
- Canonical corpus: 114 surahs, 6236 ayahs, 83668 words. All 20 refresh stages passed.
- Verification env: `ConnectionStrings__QuranDashboardTest='Host=/var/run/postgresql;Database=quran_dashboard_test;Username=mohamed'`
- Operator ordering: create db from template0 -> migrations -> `admin apply` -> `admin verify` -> `refresh apply`.

## Verification commands
- `/projects/Dashboard/App/Backend/scripts/test-backend gate-contract --no-build` (use ABSOLUTE path; run detached, it has been killed twice)
- `node scripts/test focused --backend-class <CLASS> --no-build`
- `node scripts/verify-test-policy-runner.mjs`
- Frontend (run from Frontend/quran-dashboard-ui): `node scripts/verify-playwright-policy-contract.mjs`,
  `verify-canonical-playwright-runtime.mjs`, `verify-independent-playwright-oracles.mjs`, `verify-e2e-database-contract.mjs`

## Shard-1 blocker (CORRECTED 2026-09-04)
gate-contract shard 1 does NOT crash - it HANGS. Blame collector aborts after 20 min inactivity while
running `TestRuntime.TestRuntimeCommandTests.LockHold_EarlyFailureIsOneCompactJsonLineForTheRepositoryRunner`
(191 passed before it). Shard 2 is GREEN 45/45.
The two stderr lines are RED HERRINGS, both caught and logged, never escaping:
  - `[owned-providers] ...` OwnedServiceProviderRegistry.cs:58-67
  - `[process-state] ...`   ProcessGlobalStateScope.cs:134-143
ROOT CAUSE (resolved by #168): `LockHold_EarlyFailureIsOneCompactJsonLineForTheRepositoryRunner` used the
REAL environment resolver. Once I provisioned quran_dashboard_test and started exporting
ConnectionStrings__QuranDashboardTest, `lock hold` found a live DB, took a real exclusive lock and blocked
forever instead of reporting `target.connection-string.missing`. The hang was triggered by my own
provisioning, NOT by a defect #161 shipped. #168 fixed it by injecting `_ => null` as the resolver.
LESSON: tests asserting "missing configuration" behaviour can silently invert once a real test DB exists.

## agy EVALUATION VERDICT (2026-09-04)
Capable implementer, unreliable finisher. Ran #159 end-to-end on `gemini-3.8-flash-high`, 664 steps.
GOOD: correct scope; avoided the fingerprint trap unaided by reusing #158's bounded lifecycle
(shard1 244 tests in 2.08min vs 240 in 2.59min baseline); registry clean; policy contract passes;
added tests rather than weakening any.
BAD: never finished - looped on "I will wait for it to complete", relay status `failed`, no commit,
never ran /code-review. Its focused run reported 3/3 GREEN on a class that FAILS in the full shard -
a false green. NEVER trust agy's focused results; always validate with a full lane.
NO SUBAGENTS: all 664 steps had has_subtrajectory=0 and `agy agents` lists none. The `code-review`
skill needs PARALLEL sub-agents, so agy cannot run it as designed; it also asks for a fixed point,
which nothing can answer in headless --print.

## agy (Google Antigravity CLI) notes - 2026-09-04
agy 1.1.25, model `gemini-3.8-flash-high`. Three dispatch attempts of #159, ALL aborted, ZERO files
written. Nothing landed. Findings:
1. Headless `--print` auto-denies tool permissions -> 0 files. Needs `--dangerously-skip-permissions`
   (user approved 2026-09-04) or exact `command(...)` allow-rules in
   `~/.gemini/antigravity-cli/settings.json`.
2. SKILL INVOCATION: use the SLASH FORM `/implement ticket #<N> only.` - verified working in --print.
   Plain prose "Invoke implement skill for ticket #<N> only." does NOT expand. Do not pass
   `--disable-slash-commands`.
3. DO NOT COPY SKILLS ANYWHERE. The user installs skills GLOBALLY and agy resolves them itself.
   The `~/.agents/skills ... must be an absolute path` lines in agy.log are harmless noise, NOT a
   failure. I wrongly concluded skills were unreachable, copied implement/code-review/tdd into the
   worktree and added git exclude rules; all of that has been reverted at the user's instruction.
4. Recurring `grep_handler.go: app root path missing` appears in agy.log; effect unknown.
5. Resume a conversation with `--conversation <id>` (relay prints it), NOT codex's `--session`.
6. Fallback only if ever needed: a self-contained 153-line brief exists at
   `/home/mohamed/.claude/jobs/46c829cf/tmp/159/brief-unused-long.txt`. Not used; the skill route is
   the correct one.

## CRITICAL ENV FACT (learned 2026-09-04 the hard way)
`/tmp` IS tmpfs ON THIS MACHINE. A reboot DELETED every worktree under `/tmp/quran-dashboard-issue-*`.
Put worktrees in `/home/mohamed/qdb-worktrees/<name>` instead. Branches survive (they live in .git),
so a lost worktree can be recreated with `git worktree add <persistent-path> <branch>`.
Also: background lanes do NOT survive machine sleep/suspend - they die reporting "was stopped".

## Worker dispatch recipe
1. `git worktree add -b codex-issue-<N>-from-149 /home/mohamed/qdb-worktrees/issue-<N> <tip>`  (NOT /tmp - tmpfs!)
2. DO NOT copy any skill into the worktree. Skills are installed globally by the user and the CLIs
   resolve them themselves. (User removed all speckit skills in `b0bca7d8`, so the old
   `speckit-implement` misroute is gone anyway.)
3. brief file containing exactly: `Invoke implement skill for ticket #<N> only.`
4. `ConnectionStrings__QuranDashboardTest=... node /home/mohamed/.agents/skills/codex-delegate/scripts/relay.mjs --brief brief.txt --cd <worktree> --model gpt-5.6-sol --effort high --out-dir <dir> --timeout 3h`
5. Remediation (never re-dispatch): same command with `--session <threadId>` and an evidence brief.

## Recurring worker behaviour
Workers often finish WITHOUT committing (#157, #161 remediation). Orchestrator may create the landing
commit from the reviewed working state without editing contents.
